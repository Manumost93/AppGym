package com.appgym.common.dto;

/**
 * CLASS cubre clases de gimnasio/crossfit con instructor y cupo; COURT cubre
 * reserva de pista (padel). Ambas comparten el mismo modelo de slot + reserva.
 */
public enum ActivityType {
    CLASS,
    COURT
}
